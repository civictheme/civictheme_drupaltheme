{#
/**
 * @file
 * CivicTheme implementation to display a CivicTheme Reference card.
 *
 * Node view modes, node preprocessing and rendering of card components are being utilised for card paragraphs
 * that reference nodes - we just need to render the provided variable.
 *
 */
#}
{{ reference_card }}
