{#
/**
 * @file
 * CivicTheme theme implementation of views exposed form.
 */
#}
{% if q is not empty %}
  {#
  This ensures that, if clean URLs are off, the 'q' is added first,
  as a hidden form element, so that it shows up first in the POST URL.
  #}
  {{ q }}
{% endif %}
{# Group filter is printed if there is more than 1 exposed filter in the view #}
{% if group_filter %}
  {% include '@molecules/group-filter/group-filter.twig' with {
    filter_title: filter_title,
    desktop_breakpoint: '>=m',
    filters: form,
  } %}
{% else %}
  {{ form }}
{% endif %}
