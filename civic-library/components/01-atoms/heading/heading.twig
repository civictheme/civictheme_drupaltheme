{#
/**
 * @file heading!
 *
 * Available variables:
 * - title: [string] The text of title.
 * - level: [string] 1 | 2 | 3 | 4 | 5 | 6 The default is 2.
 * - modifier_class: [string] Classes to modify the default component styling.
 */
#}

{% set level = level|default('2') %}

{# Defaults to h2 (level 2) if no heading level is supplied. #}
<h{{ level }} class="civic-heading {{ modifier_class }}">
    {{- title|raw -}}
</h{{ level }}>
